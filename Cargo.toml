[package]
name = "nazuki"
version = "0.1.0"
authors = ["n4o847"]

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2.40"

[package.metadata.wasm-pack.profile.release]
wasm-opt = ["-O", "--enable-mutable-globals"]
